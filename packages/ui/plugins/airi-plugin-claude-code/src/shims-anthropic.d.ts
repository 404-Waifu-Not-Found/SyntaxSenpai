declare module '@anthropic-ai/claude-code' {
  // Minimal shim to avoid hard dependency on upstream types during workspace typechecking.
  // Expand as needed if features from the package are used in a typed way.
  export type HookInput = any
  const _default: any
  export default _default
}
