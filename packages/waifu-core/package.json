{
  "name": "@syntax-senpai/waifu-core",
  "version": "0.0.1",
  "description": "Waifu model, personality engine, and built-in roster",
  "private": true,
  "main": "dist/index.js",
  "react-native": "src/index.ts",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "pretest": "pnpm run build",
    "test": "node dist/test-runner.js",
    "test:unit": "vitest run --environment node"
  },
  "dependencies": {
    "@syntax-senpai/ai-core": "workspace:*",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
